<%- include('../includes/head.ejs') %>
    <style>
        body {
            background-color: #F2F2F2; /* 🎨 Fond légèrement gris */
        }
        .icon-container {
            width: 60px;
            height: 60px;
            background-color: #F3F4F6; /* 🎨 Background color for icons */
            border-radius: 50%; /* 🔵 Make the container circular */
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto;
            border: 2px solid #B0B0B0; /* 🔲 Border for round icons */
        }
        .icon {
            font-size: 24px; /* 🔠 Icon size */
        }
    </style>
</head>

<body class="flex items-center justify-center min-h-screen">
    <div class="w-96 bg-white rounded-3xl p-4 shadow-lg">
        <!-- navbar -->
        <%- include('./index/navbar.ejs') %>
        <!-- Profile and statistics -->
        <%- include('./index/profile.ejs') %>
        <!-- HOME -->
        <%- include('./index/home.ejs') %>
        <!-- LES PLUS UTILISÉES -->
        <div class="bg-gray-900 rounded-3xl shadow-lg"><%- include('./index/apps-sites.ejs') %></div>
        <!-- Footer navigation -->
        <%- include('./index/footer.ejs') %>
    </div>

<%- include('../includes/end.ejs') %>